package com.farmasol.backend.model.enums;

/** Estado de revisión de una foto de receta médica subida para una línea de pedido. */
public enum EstadoReceta {
    PENDIENTE_REVISION,
    APROBADA,
    RECHAZADA
}
